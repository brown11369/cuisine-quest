name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: ["main", "dev"]
  pull_request:
    branches: ["main", "dev"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # 1️⃣ Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4

      # 2️⃣ Setup Node.js with caching
      - name: Use Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      # 3️⃣ installs dependencies (npm ci can also be used if you do not have a package-lock.json)
      - name: Install dependencies
        run: npm ci

      # 4️⃣ Run ESLint (fail on warnings)
      - name: Run ESLint
        run: npm run lint -- --max-warnings=0

      # # 5️⃣ Run tests (optional, remove if you don't have any)
      # - name: Run tests
      #   run: npm test

      # 6️⃣ Deploy to Vercel only if lint/tests pass
      - name: Deploy to Vercel
        if: github.ref == 'refs/heads/main' && success()
        run: curl -X POST ${{ secrets.VERCEL_DEPLOY_HOOK }}
